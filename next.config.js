/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['links.papareact.com', 'https://images.unsplash.com']
  }
}

module.exports = nextConfig
